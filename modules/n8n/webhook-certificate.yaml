apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: n8n-webhook-cert
  namespace: n8n
spec:
  secretName: n8n-webhook-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - webhook.cantalay.com
